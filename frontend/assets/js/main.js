const CFG=window.TEO_CONFIG||{};
const DEFAULT_SETTINGS={siteName:'Lại Húp File',studio:'Téo Studio',avatar:'assets/img/default-avatar.svg',heroTitle:'Téo Studio',heroText:'Kho file, code, script và tài nguyên được Téo cập nhật.',announcementTitle:'Thông báo từ Téo Studio',announcementText:'Hãy đọc kỹ lưu ý trước khi tải và luôn kiểm tra file trước khi sử dụng.',announcementEnabled:true,groupLink:'#',adminContact:'#'};
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const apiUrl=p=>(CFG.API_BASE_URL||'').replace(/\/$/,'')+p;
async function apiJson(p,o={}){const r=await fetch(apiUrl(p),{...o,headers:{'content-type':'application/json',...(o.headers||{})}});let x={};try{x=await r.json()}catch{}if(!r.ok)throw Error(x.error||`API ${r.status}`);return x}
let products=[],tags=[],site={...DEFAULT_SETTINGS};
function normalize(p){return {...p,downloadLink:p.download_link??p.downloadLink,createdAt:p.created_at??p.createdAt,date:p.date||p.created_at||'Mới đăng',thumb:p.thumb||'assets/img/default-avatar.svg'};}
async function load(){try{const [a,b,c]=await Promise.all([apiJson('/api/products'),apiJson('/api/tags'),apiJson('/api/settings')]);products=(a.products||[]).map(normalize);tags=['Tất cả',...(b.tags||[]).map(x=>x.name)];site={...DEFAULT_SETTINGS,...(c.settings||{})};return true}catch(e){console.warn(e);products=[];tags=['Tất cả'];site={...DEFAULT_SETTINGS};return false}}
function renderVideo(){const root=$('#avatarMedia');if(!root)return;const u=CFG.AVATAR_VIDEO_URL||site.avatarVideoUrl||'';root.innerHTML=u?`<div class="avatar-video-frame"><video id="avatarVideo" autoplay loop muted playsinline preload="metadata"><source src="${esc(u)}" type="video/mp4"></video><button id="videoSoundBtn" class="video-sound-btn" type="button" aria-label="Bật/tắt âm thanh">🔇</button></div>`:'';const v=root.querySelector('video'),b=root.querySelector('#videoSoundBtn');if(!v)return;v.play().catch(()=>{});if(b)b.onclick=async()=>{try{v.muted=!v.muted;await v.play();b.textContent=v.muted?'🔇':'🔊';}catch(e){v.muted=true;b.textContent='🔇';}};}
function renderProducts(tag='Tất cả',q=''){let a=products.slice().sort((x,y)=>new Date(y.createdAt||0)-new Date(x.createdAt||0));if(tag!=='Tất cả')a=a.filter(x=>x.game===tag);q=q.toLowerCase().trim();if(q)a=a.filter(x=>`${x.title} ${x.game} ${x.description}`.toLowerCase().includes(q));$('#productGrid').innerHTML=a.map(x=>`<article class="product-card" onclick="location.href='detail.html?id=${encodeURIComponent(x.id)}'"><div class="product-thumb"><img src="${esc(x.thumb)}" alt="" onerror="this.onerror=null;this.src='assets/img/default-avatar.svg'"></div><div class="product-body"><div class="product-meta"><span class="type-pill free">FILE</span><span>${esc(x.game||'Khác')}</span></div><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p><div class="product-foot"><span>📅 ${esc(x.date)}</span><span>👁 ${Number(x.views||0).toLocaleString('vi-VN')}</span></div></div></article>`).join('');$('#emptyState')?.classList.toggle('hidden',!!a.length);}
function notice(){const m=$('#noticeModal');if(!m||site.announcementEnabled===false)return;if(Date.now()<Number(localStorage.lhf_notice_muted||0))return;$('#noticeTitle').textContent=site.announcementTitle;$('#noticeText').textContent=site.announcementText;let n=3;m.classList.add('open');const c=$('#noticeCount'),ok=$('#closeNotice'),mute=$('#muteNotice');const t=setInterval(()=>{n--;if(n>0)c.textContent=`Vui lòng chờ ${n} giây...`;else{clearInterval(t);c.textContent='Bạn có thể đóng thông báo.';ok.disabled=false;mute.disabled=false}},1000);ok.onclick=()=>m.classList.remove('open');mute.onclick=()=>{localStorage.lhf_notice_muted=Date.now()+3600000;m.classList.remove('open')};}
function home(){if($('#studioName'))$('#studioName').textContent=site.studio.toUpperCase();if($('#studioAvatar'))$('#studioAvatar').src=site.avatar||DEFAULT_SETTINGS.avatar;$('#heroTitle').textContent=site.heroTitle;$('#heroText').textContent=site.heroText;renderVideo();const rail=$('#tagRail');rail.innerHTML=tags.map((x,i)=>`<button class="tag ${i?'':'active'}" data-tag="${esc(x)}">${esc(x)}</button>`).join('');rail.querySelectorAll('.tag').forEach(b=>b.onclick=()=>{rail.querySelectorAll('.tag').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderProducts(b.dataset.tag,$('#searchInput').value)});$('#searchInput').oninput=e=>renderProducts($('.tag.active')?.dataset.tag||'Tất cả',e.target.value);$('#newestBtn').onclick=()=>document.querySelector('#products')?.scrollIntoView({behavior:'smooth'});$('#menuBtn').onclick=()=>{$('#drawer').classList.add('open');$('#drawerBackdrop').classList.add('open')};const close=()=>{$('#drawer').classList.remove('open');$('#drawerBackdrop').classList.remove('open')};$('#drawerClose').onclick=close;$('#drawerBackdrop').onclick=close;$('#notificationBtn').onclick=()=>$('#notificationModal').classList.add('open');document.querySelectorAll('[data-close]').forEach(x=>x.onclick=()=>$('#'+x.dataset.close).classList.remove('open'));renderProducts();notice();if($('#supportBox'))$('#supportBox').href=site.adminContact||'#';if($('#groupBox'))$('#groupBox').href=site.groupLink||'#';document.title=`${site.siteName} — ${site.studio}`;}
async function detail(){const id=new URLSearchParams(location.search).get('id');let p=products.find(x=>x.id===id);try{if(id)p=normalize((await apiJson('/api/products/'+encodeURIComponent(id))).product)}catch{}if(!p||!$('#detailPage'))return;$('#detailPage').innerHTML=`<div class="detail-card"><div class="detail-thumb"><img src="${esc(p.thumb)}" onerror="this.onerror=null;this.src='assets/img/default-avatar.svg'"></div><div class="detail-body"><div class="product-meta"><span class="type-pill free">FILE</span><span>${esc(p.game||'Khác')}</span></div><h1>${esc(p.title)}</h1><p>${esc(p.description||'')}</p><div class="detail-info"><span>📅 ${esc(p.date||'')}</span><span>👁 ${Number(p.views||0).toLocaleString('vi-VN')}</span></div><div class="rule-box">⚠ <b>Lưu ý:</b> ${esc(p.warning||'Kiểm tra file trước khi dùng.')}</div><button class="btn primary full" onclick="location.href='download.html?id=${encodeURIComponent(p.id)}'">⬇ Xem link tải</button></div></div>`}
async function download(){const id=new URLSearchParams(location.search).get('id');let p=products.find(x=>x.id===id);try{if(id)p=normalize((await apiJson('/api/products/'+encodeURIComponent(id))).product)}catch{}if(!p||!$('#downloadPage'))return;$('#downloadPage').innerHTML=`<div class="download-card"><span class="eyebrow">TẢI FILE</span><h1>${esc(p.title)}</h1><div class="rule-box">⚠ <b>Ghi chú:</b> ${esc(p.warning||'Kiểm tra file trước khi dùng.')}</div><a class="btn primary full" href="${esc(p.downloadLink||'#')}" target="_blank" rel="noopener">⬇ Mở link tải</a></div>`}
window.addEventListener('DOMContentLoaded',async()=>{if($('#productGrid')){await load();home()}else if($('#detailPage')){await load();detail()}else if($('#downloadPage')){await load();download()}});
